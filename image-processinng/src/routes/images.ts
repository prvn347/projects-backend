import { Router } from "express";

const router = Router();

router.post("/", async () => {
  try {
  } catch (error) {}
});
router.post("/:id/transform", async () => {
  try {
  } catch (error) {}
});

router.post("/:id", async () => {
  try {
  } catch (error) {}
});
router.post("/", async () => {
  try {
  } catch (error) {}
});
